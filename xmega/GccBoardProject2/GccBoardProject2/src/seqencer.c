#include <asf.h>
#include "sequencer.h"

void decrement_mode_counter(void)
{
	//time_to_exit_current_mode = time_to_exit_current_mode - 1;
	
	//if (time_to_exit_current_mode == 0)
	//	exitmode(current_mode);
}